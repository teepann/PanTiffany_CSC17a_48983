/* 
 * File:   prob_12_8.cpp
 * Author: Tiffany Pan
 * Created on September 30, 2015, 12:53 PM
 * Purpose: "arry/file functions"
 * Homework: Complete this assignment
 */

//System Libraries
#include <iostream> //input/output stream library
#include <string> //string library
#include <iomanip> //input/output manipulation
#include <cctype> //cctype library
#include <cstdlib> //c standard library
using namespace std; //utilize standard name-space directly 
 
//User Libraries
//Global Constants
//Function Prototypes

/*
 * write a function named arrayToFile. The function should accept 3 
 * arguments: the name of afile, a pointer to an int array,
 * the size of the array. the function should open the specified
 * file in binary mode, write the contents of the array to the file,
 * and then close the file.
 * write antoehr function named file ToArray. Thsi function should accept
 * 3 arguments: the name of a file, apointer to an int array, the size of array.
 * the function should open the specified fiel in binary mode, read its contents
 * into the array, and then clsoe the file.
 * write a complete prgoram that demonstrates these functions by using the 
 * arrayToFile function to write an array to a file, and then using the 
 * fileToArray functionto read the data from the same file.
 * After the data are read from the file into the array, display
 * the array's contents on the screen.
 */
int main(int argc, char** argv) {

    return 0;
}

